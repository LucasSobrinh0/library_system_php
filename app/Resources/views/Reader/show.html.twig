{% extends 'base.html.twig' %}

{% block body %}
  <h1>Detalhes do Livro</h1>
  <p><strong>Título:</strong> {{ reader.name }}</p>
  <p><strong>ISBN:</strong>  {{ reader.phone }}</p>
  <p><strong>Ano:</strong>   {{ reader.email }}</p>
  <p><strong>Autor:</strong> {{ reader.dateOfBirth|date('d/m/Y') }}</p>
  <p><strong>Categoria:</strong> {{ reader.cpf }}</p>
  <a href="{{ path('reader_index') }}">Voltar</a>
  <a href="{{ path('reader_edit', {'id': reader.id}) }}">Editar</a>
  {% include 'Reader/_delete_form.html.twig' with { 'form': delete_forms } %}
{% endblock %}
